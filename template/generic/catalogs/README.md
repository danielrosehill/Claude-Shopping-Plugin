# Catalogs

Drop catalog screenshots / saved listings into per-vendor subdirectories named after each vendor (lowercase, kebab-case). Claude reads PNG/JPG screenshots directly via `/shopping:catalog-to-json`.
