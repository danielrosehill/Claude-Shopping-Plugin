# Outputs

`/shopping:recommend` writes the ranked recommendation report here as Typst source plus compiled PDF (e.g. `recommendations.typ` + `recommendations.pdf`).

Dated re-runs (`/shopping:recompare`) live under `from-ai/recommendations/recommendations-YYYY-MM-DD.md`; the latest can be re-rendered to PDF here.
